version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14

    steps:
      - checkout

      # Add your build steps here, such as installing dependencies and running tests
      - run:
          name: Install dependencies
          command: npm install

  deploy:
    docker:
      - image: circleci/node:14

    steps:
      - checkout
      
      - run:
          name: Log SSH_PRIVATE_KEY
          command: echo "$SSH_PRIVATE_KEY"

       # Create SSH directory and set up SSH key
      - run:
          name: Set up SSH key
          command: |
            mkdir -p ~/.ssh
            echo "$SSH_PRIVATE_KEY" > /home/circleci/.ssh/crud.pem
            chmod 400 /home/circleci/.ssh/crud.pem

      # Example SSH command
      - run:
          name: SSH into server
          command: ssh -vvv -o StrictHostKeyChecking=no -i /home/circleci/.ssh/crud.pem ubuntu@ec2-3-90-250-145.compute-1.amazonaws.com

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: main
